{
  "name": "pilane-web",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "bunx --bun astro dev",
    "build": "bunx --bun astro build",
    "preview": "astro preview",
    "astro": "astro",
    "predev": "bun run ./scripts/generateImageImports.ts",
    "prebuild": "bun run ./scripts/generateImageImports.ts",
    "postbuild": "bunx @divriots/jampack ./dist"
  },
  "dependencies": {
    "@astrojs/tailwind": "^5.1.5",
    "astro": "^5.1.9",
    "tailwindcss": "^3.4.17"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0"
  }
}